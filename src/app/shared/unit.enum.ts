export enum MetricUnit {
  Centimeters,
  Millimeters,
}

export enum ImperialUnit {
  Thou,
  Inches,
  Feet,
}

export enum TimeUnit {
  Seconds,
  Milliseconds
}

export enum MediaUnit {
  Pixels,
  Picas,
  Points
}
